import 'package:flutter/material.dart';

class InfoDesaItem extends StatelessWidget {
  const InfoDesaItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
